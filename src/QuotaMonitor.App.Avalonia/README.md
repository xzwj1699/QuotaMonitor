# QuotaMonitor.App.Avalonia

Future single desktop shell for Windows and macOS.

This project is the future Avalonia shell and should reference `../QuotaMonitor.Core/QuotaMonitor.Core.csproj`.

The WinForms app remains only as a temporary compatibility shell until this project reaches feature parity.
